# NetBeans / build artifacts intermedios (no hacen falta, el WAR ya viene armado)
build/
nbproject/private/

# Compilados (excepto el WAR final, que SÍ se sube porque el Dockerfile lo usa)
*.class
*.jar
!web/**/*.jar
!dist/Neins.war

# Sistema
.DS_Store
Thumbs.db

# IDE
.vscode/
*.iml
